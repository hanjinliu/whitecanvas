===========
whitecanvas
===========

:mod:`whitecanvas` is a type safe and backend independent plotting library for Python.

Source
======

`Jump to GitHub repository <https://github.com/hanjinliu/whitecanvas>`_.

Installation
============

:mod:`whitecanvas` is available on PyPI.

.. code-block:: bash

    pip install whitecanvas -U

You can also install backend optional dependencies using one of the following commands.

.. code-block:: bash

    pip install whitecanvas[matplotlib] -U
    pip install whitecanvas[pyqtgraph] -U
    pip install whitecanvas[vispy] -U
    pip install whitecanvas[plotly] -U
    pip install whitecanvas[bokeh] -U

Contents
========

.. toctree::
    :maxdepth: 1

    ./main/quick_start
    ./main/line_layers
    ./main/face_layers
    ./main/layer_groups
    ./main/categorical


Indices and tables
==================

* :ref:`genindex`
* :ref:`modindex`
* :ref:`search`
